# WarcraftLogs API v2 credentials (client-credentials flow).
# Get yours: https://www.warcraftlogs.com/api/clients/ -> "Create Client"
# Copy this file to .env (same folder, or in your workdir) and fill in.
# .env is gitignored — NEVER commit credentials.
WCL_CLIENT_ID=
WCL_CLIENT_SECRET=
